name: Legacy gh-pages Branch Deploy (No-op)

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]
  workflow_dispatch:

jobs:
  configure:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow intentionally disabled
        run: |
          echo "Legacy gh-pages branch deployment is disabled."
          echo "Use deploy-frontend-pages.yml (artifact + deploy-pages) on main only."
